<%= simple_form_for :user_sessions, { url: user_sessions_path } do |f| %>
  <%= f.input :email, label: '@email' %>
  <%= f.input :password, label: t('form.pass') %>
  <%= f.submit t('form.enter') %>
<% end %>
