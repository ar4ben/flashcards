<%= simple_form_for :user_sessions, { url: home_user_sessions_path } do |f| %>
  <%= f.input :email, label: '@email' %>
  <%= f.input :password, label: t('.pass') %>
  <%= f.submit t('.enter') %>
<% end %>
